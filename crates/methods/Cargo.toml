[package]
name = "methods"
version = { workspace = true }
edition = { workspace = true }

[build-dependencies]
risc0-build = { version = "1.2.5" }

[package.metadata.risc0]
methods = ["mmr-build", "mmr-benchmark", "world-coin"]
